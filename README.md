# Rainworld_skip_SleepAndDeathScreen
Skips the rainworld sleep&death screen and continues automatically to next cycle


to build you need to reference the DLLs in debug + the HOOKS and PUBLIC assembly one in bepinex, and assembly first pass from the game